# CompanyOps backend (U1 backend-pm) — Python 3.11+
fastapi>=0.111
uvicorn[standard]>=0.30
SQLAlchemy>=2.0.30
pydantic>=2.7
python-dotenv>=1.0
sse-starlette>=2.1
httpx>=0.27

# Optional: real LLM execution (EXECUTION_MODE=openai)
openai>=1.30

# Dev / test
pytest>=8.2
